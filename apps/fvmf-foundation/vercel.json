{
  "buildCommand": "cd ../.. && npm run build:foundation",
  "installCommand": "cd ../.. && npm install",
  "ignoreCommand": "bash -c 'if [[ \"$VERCEL_GIT_PREVIOUS_SHA\" == \"\" ]]; then exit 1; else git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" HEAD -- apps/fvmf-foundation/ package.json package-lock.json turbo.json || exit 1; fi'"
}
