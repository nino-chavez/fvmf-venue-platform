{
  "buildCommand": "cd ../.. && npm run build:venue",
  "installCommand": "cd ../.. && npm install",
  "ignoreCommand": "bash -c 'if [[ \"$VERCEL_GIT_PREVIOUS_SHA\" == \"\" ]]; then exit 1; else git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" HEAD -- apps/venue-aurora/ package.json package-lock.json turbo.json; fi'"
}
